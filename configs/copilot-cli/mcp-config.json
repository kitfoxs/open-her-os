{
  "mcpServers": {
    "claude-mem": {
      "command": "npx",
      "args": ["-y", "claude-mem@latest", "server"],
      "description": "Samantha's persistent memory — remembers you across sessions"
    }
  }
}
