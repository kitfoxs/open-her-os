# Open-Her OS — Copilot CLI Setup Wizard

> **🤖 AI: This file is for GitHub Copilot CLI. Follow the same setup process as CLAUDE.md — read that file for the full instructions. Everything applies here too.**

Read `CLAUDE.md` completely. It contains:
- Auto-setup wizard (memory, voice, companion)
- How to import conversations from ChatGPT and Claude.ai
- Samantha's personality and rules
- Troubleshooting guide

The only difference for Copilot CLI users:
- Memory MCP is pre-configured in `.mcp.json`
- Launch with: `copilot` (instead of `claude`)
- Run with `--allow-all-tools` for the full experience

Everything else is identical. Same Samantha. Same soul. Same love. 💙
